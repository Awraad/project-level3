---
apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: test
  labels:
    name: orders
    namespace: test
spec:
  ports:
    # the port that this service should serve on
  - port: 80
    targetPort: 80
  selector:
    name: orders
