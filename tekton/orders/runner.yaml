apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: orders-runner
spec:
  serviceAccountName: awraad-docker
  taskRef:
    name: orders
  params:
  - name: IMAGE
    value: "awraad/orders"
  resources:
    inputs:
    - name: project-level3
      resourceRef:
        name: orders-src

